export default function ContactPage() {
  return (
    <div className="mx-auto max-w-2xl px-6 py-24 text-[var(--foreground)]">
      <h1 className="text-2xl font-semibold">Contact</h1>
      <p className="mt-4 text-[var(--muted)]">
        This page will be built in a later pass. Use the phone number on the home page
        or book an intro call from the hero.
      </p>
    </div>
  );
}
